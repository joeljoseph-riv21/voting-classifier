# voting-classifier
Built a simple voting classifier model which combines the strengths of multiple machine learning algorithms and the ensemble model achieves higher stability and accuracy than any single model alone
